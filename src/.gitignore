# environment variables
# `docker compose` reads .env from the directory holding docker-compose.yml,
# which is this one. Never commit a real key.
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
